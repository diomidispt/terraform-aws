# Secrets for this env come from the SOPS-encrypted `secrets.enc.json` in this
# folder (decrypted at plan/apply by the sops provider) — NOT from tfvars.
# account/region come from the symlinked common.*.auto.tfvars. Nothing to set here.
